<form method="post" action="{{ path('admin_basic_email_template_delete', {'id': basic_email_template.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce modèle ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ basic_email_template.id) }}">
    <button class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">
        Supprimer
    </button>
</form>
