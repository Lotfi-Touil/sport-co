{% extends 'base.html.twig' %}

{% block title %}InvoiceStatus index{% endblock %}

{% block body %}
   {% include '_partials/back/_platform_nav.html.twig' %}

    <h1>InvoiceStatus index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for invoice_status in invoice_statuses %}
            <tr>
                <td>{{ invoice_status.id }}</td>
                <td>{{ invoice_status.title }}</td>
                <td>
                    <a href="{{ path('platform_invoice_status_show', {'id': invoice_status.id}) }}">show</a>
                    <a href="{{ path('platform_invoice_status_edit', {'id': invoice_status.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('platform_invoice_status_new') }}">Create new</a>
{% endblock %}
