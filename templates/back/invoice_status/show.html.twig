{% extends 'base.html.twig' %}

{% block title %}InvoiceStatus{% endblock %}

{% block body %}
   {% include '_partials/back/_platform_nav.html.twig' %}

    <h1>InvoiceStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invoice_status.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ invoice_status.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('platform_invoice_status_index') }}">back to list</a>

    <a href="{{ path('platform_invoice_status_edit', {'id': invoice_status.id}) }}">edit</a>

    {{ include('back/invoice_status/_delete_form.html.twig') }}
{% endblock %}
