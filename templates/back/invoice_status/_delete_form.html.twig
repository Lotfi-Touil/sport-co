<form method="post" action="{{ path('platform_invoice_status_delete', {'id': invoice_status.id}) }}" onsubmit="return confirm('Voulez vraiment supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invoice_status.id) }}">
    <button class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">
        Supprimer
    </button>
</form>
